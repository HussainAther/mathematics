import numpy as np

"""
Lyapunov's "second method" or "direct method" for ordinary differential
equations of the form:

dx/dt = ẋ = f(x)

in which the first equals sign denotes the definition of dx/dt
"""
